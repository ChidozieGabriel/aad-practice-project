<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chidozie.n.aadpracticeproject"
    >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AadApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AADPracticeProject"
        tools:ignore="AllowBackup"
        >

        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.AADPracticeProject.Splash"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.leaderboard.activity.LeaderboardActivity" />
        <activity android:name=".ui.submission.activity.SubmissionActivity" />

    </application>

</manifest>
